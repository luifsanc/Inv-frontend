/* tslint:disable:no-unused-variable */
/*
import { TestBed, async, inject } from '@angular/core/testing';
import { EquipmentDismissalService } from '../equipmentDismissal.service';

describe('Service: Out', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [EquipmentDismissalService]
    });
  });

  it('should ...', inject([EquipmentDismissalService], (service: EquipmentDismissalService) => {
    expect(service).toBeTruthy();
  }));
});*/
